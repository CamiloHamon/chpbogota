<link rel="stylesheet" href="/css/partials/videoSection.css">

<div class="video-section-container">
    <!-- Sección del video -->
    <div class="video-container">
        <iframe src="https://www.youtube.com/embed/{{videoUrl}}" title="{{videoTitle}}" frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen>
        </iframe>
    </div>

    <!-- Sección del texto -->
    <div class="video-text-container">
        <h2>{{videoTitle}}</h2>
        <h3>{{programName}}</h3>
        <p>{{description1}}</p>
        <p>{{description2}}</p>
        <a href="{{videoLink}}" class="more-videos-btn">Más videos</a>
    </div>
</div>